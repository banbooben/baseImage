# 仅手动；job 级 needs: [] 打断 stage 顺序

code-server:
  extends: .manual_only
  stage: software
  script:
    - echo "开始构建code-server镜像"
    - cd $ROOT_PATH/${project_repo_name}
    - >
      docker buildx build
      --builder builder
      --platform linux/amd64,linux/arm64
      -t ${REGISTRY}/${NAMESPACE}/software:noble-code-server
      -f ./docker_installer/dockerfiles/software/backend/code-server
      --build-arg REGISTRY=${REGISTRY}
      --build-arg NAMESPACE=${NAMESPACE}
      --secret id=CODE_SERVER_PASSWORD,env=CODE_SERVER_PASSWORD
      --push
      .

docker:
  extends: .manual_only
  stage: software
  script:
    - echo "开始构建docker镜像"
    - cd $ROOT_PATH/${project_repo_name}
    - >
      docker buildx build
      --builder builder
      --platform linux/amd64,linux/arm64
      -t ${REGISTRY}/${NAMESPACE}/software:noble-docker
      -f ./docker_installer/dockerfiles/software/backend/docker
      --build-arg REGISTRY=${REGISTRY}
      --build-arg NAMESPACE=${NAMESPACE}
      --push
      .

dbeaver:
  extends: .manual_only
  stage: software
  script:
    - echo "开始构建 DBeaver 桌面镜像"
    - cd $ROOT_PATH/${project_repo_name}
    - >
      docker buildx build
      --builder builder
      --platform linux/amd64,linux/arm64
      -t ${REGISTRY}/${NAMESPACE}/software:noble-dbeaver
      -f ./docker_installer/dockerfiles/software/desktop/dbeaver
      --build-arg REGISTRY=${REGISTRY}
      --build-arg NAMESPACE=${NAMESPACE}
      --push
      .

clash-verge:
  extends: .manual_only
  stage: software
  script:
    - echo "开始构建 Clash Verge 桌面镜像"
    - cd $ROOT_PATH/${project_repo_name}
    - >
      docker buildx build
      --builder builder
      --platform linux/amd64,linux/arm64
      -t ${REGISTRY}/${NAMESPACE}/software:noble-clash-verge
      -f ./docker_installer/dockerfiles/software/desktop/clash-verge
      --build-arg REGISTRY=${REGISTRY}
      --build-arg NAMESPACE=${NAMESPACE}
      --push
      .

chrome:
  extends: .manual_only
  stage: software
  script:
    - echo "开始构建 Chromium 镜像"
    - cd $ROOT_PATH/${project_repo_name}
    - >
      docker buildx build
      --builder builder
      --platform linux/amd64,linux/arm64
      -t ${REGISTRY}/${NAMESPACE}/software:noble-chrome
      -f ./docker_installer/dockerfiles/software/desktop/chrome
      --build-arg REGISTRY=${REGISTRY}
      --build-arg NAMESPACE=${NAMESPACE}
      --push
      .
